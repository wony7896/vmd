# 중간과제 기획서 — 브랜드 가이드 내장형 대리점 전단지 셀프제작 웹

- 이름 / 소속: 김원 / Brand Experience팀

---

## 1. 과제 배경 & 정의 (Problem)

**전 대리점의 전단지 제작 요청이 담당자 1명에게 집중되는 구조**를 바꾸려 한다.

현재 대리점은 매월 판촉 전단지가 필요할 때마다 본사 담당자(본인)에게 제작을 요청하고,
담당자가 직접 디자인해 PDF·AI 파일로 회신한다. 이 구조에서 세 가지 문제가 발생한다.

- **병목** — 전국 대리점의 요청이 1명에게 몰려 대기가 생기고, 담당자의 본업(브랜드 기획·디자인)이 잠식된다.
- **타이밍 상실** — 현장은 당장 필요한데 제작 대기 때문에 판촉 시점을 놓친다.
- **브랜드 훼손** — 기다리다 지친 대리점이 자체적으로 만들면 로고·폰트·컬러가 규격을 벗어나
  사내 브랜드 가이드가 무너진다. 이는 병목의 부작용이지, 대리점의 잘못이 아니다.

즉, **속도와 브랜드 일관성이 서로를 잡아먹고 있는 상태**다.
담당자를 거치면 느리고, 거치지 않으면 브랜드가 깨진다.
이 과제는 그 둘을 동시에 푸는 것을 목표로 한다.

> ※ 본 문서의 대리점·템플릿·프로모션 사례는 모두 가명/더미 기준으로 서술한다.

---

## 2. 현황 진단 (As-Is)

### 지금의 처리 흐름

```
대리점 요청 (카톡 / 메일 / 구두)
    → 누락 정보 되묻기 (점포명·기간·상품·요금 등)
    → 담당자 수작업 디자인
    → 시안 회신
    → 수정 요청 왕복 2~3회
    → 최종 PDF / AI 파일 전달
```

### 구체적 불편

| 구분 | 현재 상태 |
|---|---|
| **시간** | 요청 1건당 실제 디자인 작업에 **반나절~하루**. 대기까지 더하면 리드타임은 며칠 단위 |
| **반복 왕복** | "문구만 바꿔주세요", "사진 교체요" 같은 잔손질로 **수정 요청이 건당 2~3회** 발생 |
| **요청 양식** | 카톡·메일·구두로 **제각각** 들어와, 제작에 필요한 정보를 **매번 되물어야** 함 |
| **중복 작업** | 내용은 거의 같고 **점포명·날짜·상품만 다른** 건을 매번 파일을 열어 손으로 고침 |
| **확장 불가** | 대리점 수가 늘거나 프로모션이 겹치면 **처리량이 담당자 1인의 가용시간에 묶임** |

### 진단 요약

작업의 본질은 "디자인"이 아니라 **정해진 템플릿에 값을 갈아끼우는 일**이다.
창의적 판단이 필요한 부분(레이아웃·톤·브랜드 규격)은 이미 기존 템플릿에 담겨 있는데,
그 템플릿을 쓰는 일마저 담당자를 거치고 있어 병목이 생긴다.

---

## 3. AI 솔루션 구상 (To-Be)

### 무엇을 만드나

**대리점이 직접 전단지를 만드는 웹사이트**를 Claude Code로 구축한다.
핵심은 "자유로운 디자인 툴"이 아니라, **내가 만들어 둔 템플릿을 조합하는 도구**다.

1. **템플릿 블록 조합**
   기존에 제작해 둔 템플릿들을 상단(헤드라인)·본문(상품/요금)·하단(점포정보/CTA) 등
   **블록 단위로 분해**해 둔다. 대리점은 블록을 골라 조합해 한 장을 완성한다.
   → 매번 새로 디자인하지 않고, 검증된 자산을 재사용한다.

2. **브랜드 가이드 강제 (원천 차단 방식)**
   "자유롭게 만들고 나중에 검사해서 막는" 방식이 아니라,
   **가이드 안의 값만 애초에 선택 가능하게** 만든다.

   | 요소 | 통제 방식 |
   |---|---|
   | 컬러 | 지정 팔레트에서만 선택. 임의 HEX 입력 불가 |
   | 폰트 | 사내 지정 폰트만 적용. 폰트 변경 UI 자체를 제공하지 않음 |
   | 로고 | 승인된 로고 파일만 사용. 업로드·변형·재배치 불가 |
   | 레이아웃 | 템플릿이 정한 그리드 안에서만 배치 |

   → 위반이 "적발되는" 게 아니라 **구조적으로 불가능**해진다.

3. **대리점이 바꿀 수 있는 것**
   문구, 상품/요금 정보, 이미지(지정 규격), 점포명·주소·연락처, 프로모션 기간.
   즉 **매번 되묻던 정보를 대리점이 직접 입력**하게 된다.

4. **즉시 출력**
   완성 즉시 인쇄 가능한 **PDF 다운로드**. 담당자 회신 대기 없음.

### 기대 효과

| 항목 | As-Is | To-Be |
|---|---|---|
| 리드타임 | 요청 → 대기 → 제작 → 수정왕복 (며칠) | 대리점이 직접 제작, **당일/즉시** |
| 담당자 공수 | 건당 반나절~하루 | **0** (템플릿 관리·승인만) |
| 수정 왕복 | 2~3회 | 대리점이 화면에서 직접 수정 → **왕복 없음** |
| 브랜드 준수 | 자체제작 시 훼손 위험 | **위반 불가능한 구조** |
| 담당자 역할 | 제작 실무자 | **템플릿·가이드 설계자** (본업 회복) |
| 대리점 만족도 | 요청·대기·불만 | 원하는 걸 직접, 바로 |

### 핵심 범위 — 이것만은 반드시 되게 할 핵심 1가지

> **내 템플릿 블록을 조합 → 문구·이미지 교체 → PDF 다운로드까지 한 흐름을 끝까지 완성한다.**
> 단, 컬러·폰트·로고는 브랜드 가이드 밖의 값이 **선택지에 아예 존재하지 않는다.**

4일차까지는 템플릿 소수(더미)로 이 한 흐름을 완주하는 데 집중한다.
템플릿 수 확장, 대리점 계정·권한 관리, 승인 워크플로우, 인쇄소 연동은 이후 과제로 둔다.

### 구현 메모 (4일차 진행 방향)

**가장 큰 전제 — 기존 템플릿은 Illustrator(.ai) 파일이다.**
.ai는 웹에서 직접 편집·렌더링할 수 없으므로, **웹이 다룰 수 있는 형식으로 한 번 전환**하는 단계가 필요하다.
이 전환을 어떻게 하느냐가 이 과제의 성패를 가른다.

- **.ai → SVG 내보내기** 로 전환한다. SVG는 벡터를 그대로 유지하면서
  브라우저가 읽고, 텍스트·이미지를 교체할 수 있고, 인쇄 품질 PDF로 다시 뽑을 수 있다.
- **내보낼 때 텍스트를 윤곽선(outline)으로 변환하지 않는다.** 윤곽선 처리하면 글자가 도형이 되어
  문구 교체가 불가능해진다. **살아있는 텍스트 상태로 내보내는 것이 필수 조건**이다.
- 전환한 SVG에서 **바꿀 자리마다 id를 부여**한다 (예: `headline`, `product_name`, `price`,
  `store_name`, `main_image`). 웹은 이 id의 내용만 교체한다 — 나머지 디자인은 건드릴 수 없다.
- **블록 분해**: 상단·본문·하단을 각각 별도 SVG 조각으로 내보내 조합 단위로 만든다.
- **폰트**: 지정 폰트를 웹폰트로 등록해 화면과 출력이 같게 한다. 폰트 교체 UI는 제공하지 않는다.
- **PDF 출력**: 조합된 SVG를 브라우저에서 렌더링해 PDF로 출력하면 벡터 품질이 유지된다.
- 브랜드 가이드(컬러 팔레트·지정 폰트·로고 파일·금지 규칙)는 **설정 파일 한 곳**에 정의하고
  UI가 그 파일만 참조하게 해, 가이드가 바뀌면 한 곳만 고치면 되도록 한다.
  컬러는 **화면용 RGB 값과 인쇄용 공식 CMYK 값을 한 쌍으로** 정의한다 (아래 인쇄 규격 참조).
- 실제 브랜드 자산·대리점 데이터는 쓰지 않고, **동일 구조의 더미 데이터**로 검증한다.

> **4일차에 먼저 확인할 것**: 보유한 .ai 템플릿 1종을 SVG로 내보냈을 때
> 텍스트가 살아있고 레이아웃이 깨지지 않는지. 여기가 통과되면 나머지는 반복 작업이다.
> (기존 .ai 파일은 계속 **원본 마스터**로 두고, 웹용 SVG는 거기서 파생된 사본으로 관리한다.)

### 출력 형식 — 기존에 보내던 .ai 파일은 어떻게 되나

현재는 대리점에 **PDF와 AI 파일을 함께** 전달하고 있다. 웹으로 전환하면 이 부분이 달라진다.

- **웹에서 .ai 파일을 직접 생성하는 것은 불가능하다.** .ai는 Adobe 독점 형식이다.
- 대신 **일러스트레이터가 그대로 열어 편집할 수 있는 형식**으로 내보낸다.
  - **PDF** — 벡터와 텍스트가 살아있는 상태로 Illustrator에서 열려 편집된다. 인쇄 입고에도 그대로 쓴다.
  - **SVG** — Illustrator 네이티브 지원. 열어서 `다른 이름으로 저장 → .ai`로 즉시 변환된다.
  - 즉 .ai가 꼭 필요한 경우에도 **담당자가 한 번 저장하면 끝**이며, 웹의 제약이 되지 않는다.

**다만 더 중요한 것은 "대리점에 .ai가 왜 필요한가"다.**

| 용도 | 판단 |
|---|---|
| 인쇄소 입고용 | **PDF로 충분하다.** 대부분의 인쇄소가 PDF 입고를 표준으로 받는다 |
| 대리점의 추가 편집용 | **오히려 막아야 한다.** 편집 가능한 원본을 넘기는 순간 브랜드 가이드 강제라는 이 과제의 전제가 무너진다 |

**결론 — 출력은 인쇄용 PDF 한 종으로 간다.**

- 대리점에 전달되는 산출물은 **PDF뿐이며, 편집 가능한 원본(.ai)은 넘기지 않는다.**
- 이는 기능 축소가 아니라 **의도된 설계**다. 편집 가능한 원본이 나가지 않아야
  브랜드 가이드 강제가 전달 이후까지 유지된다. .ai를 함께 주던 기존 방식은
  가이드 이탈의 통로이기도 했다.
- 대리점이 내용을 고치고 싶으면 **웹에서 다시 만들면 된다.** 재제작 비용이 0에 가까우므로
  "수정본을 다시 받는" 왕복 자체가 사라진다.
- .ai가 반드시 필요한 예외(특수 인쇄 등)는 담당자가 PDF를 Illustrator에서 열어 변환해 처리한다.
  예외 처리이므로 웹의 요구사항에서는 제외한다.

---

### 인쇄 규격 — CMYK와 재단선 (가장 까다로운 지점)

브라우저에서 PDF를 뽑는 방식의 **유일한 실질적 제약**이 여기다. 미리 정리해 둔다.

**① 재단선(bleed) — 해결 가능하다**

CSS 표준에는 `@page { marks: crop; bleed: 3mm }` 가 있으나 **브라우저가 이를 무시한다.**
대신 **재단여백을 포함한 크기로 페이지를 잡고 크롭마크를 직접 그려 넣으면** 된다.

```
A4 210×297mm  →  216×303mm (사방 3mm 여백 포함)
배경·이미지는 재단선 끝까지 채우고, 크롭마크는 템플릿 SVG에 포함
```

템플릿 SVG를 만들 때 한 번만 잡아두면 이후 자동 적용되므로 장애물이 아니다.

**② CMYK — 브라우저로는 불가능하다**

브라우저 렌더링은 **무조건 RGB(sRGB)** 이며, 브라우저 PDF 출력도 RGB로 나온다.
선택지는 세 가지다.

| 방법 | 설명 | 난이도 |
|---|---|---|
| 인쇄소가 변환 | RGB PDF로 입고하고 인쇄소가 변환. 소량 디지털 인쇄는 대부분 수용 | 가장 쉬움 |
| 후처리 자동 변환 | 생성된 RGB PDF를 CMYK로 변환하는 단계를 서버에 추가 | 중간 |
| **공식 CMYK 값 병기** | 브랜드 가이드의 **공식 CMYK 값**을 팔레트에 함께 정의하고, 자동변환에 맡기지 않고 그 값으로 강제 매핑 | 정확도 최선 |

**③ 채택 방식 — 공식 CMYK 값으로 강제 매핑 (위 표의 3번)**

선명한 브랜드 레드 계열은 RGB → CMYK **자동변환에서 가장 크게 틀어지는 색**이다.
브랜드 가이드를 지키게 하려고 만든 도구가 정작 출력물에서 색을 어기면 과제 취지가 무너진다.

**다행히 사내 브랜드 가이드에 공식 CMYK 값이 이미 정의되어 있다.**
따라서 변환 알고리즘에 색을 맡길 이유가 없다. 팔레트를 다음과 같이 **값의 쌍으로** 정의한다.

```
brand_red:
  rgb  : 화면 표시용 (가이드 정의값)
  cmyk : 인쇄 출력용 (가이드 정의 공식값)
```

- 화면에서는 RGB, PDF 출력에서는 **가이드에 정의된 CMYK 값을 그대로 지정**한다.
- 자동변환이 아니라 **정해진 값을 찾아 쓰는 것**이므로 색이 틀어질 여지가 없다.
- 대리점은 어차피 팔레트 안에서만 색을 고르므로, 고른 색마다 올바른 인쇄 값이 따라붙는다.

→ 색 정확도 문제는 **가이드 값을 설정 파일에 옮겨 적는 작업**으로 해결된다.

**④ 4일차 진행 방침**

- **팔레트는 처음부터 RGB·CMYK 쌍 구조로 만든다.** 값을 옮겨 적는 것뿐이라 비용이 거의 없고,
  나중에 인쇄 대응을 붙일 때 구조를 다시 뜯지 않아도 된다.
- 다만 **PDF 출력 파이프라인의 CMYK 처리는 4일차 범위에 넣지 않는다.**
  먼저 **RGB PDF로 전체 흐름을 완주**해 동작을 확인한다.
- 재단선은 템플릿 SVG 제작 단계에서 바로 반영한다 (비용이 거의 들지 않음).
- 폰트는 PDF에 임베딩되는지만 확인한다 (브라우저 출력에서 기본 처리된다).
- 이후 실제 인쇄소가 **무엇을 입고받는지 확인한 뒤** CMYK 처리 방식을 확정한다.
