# 작업 인계 — 어디까지 왔고 무엇이 남았나

> 2026-09-17 기준. 새 세션이 이 프로젝트를 이어받을 때 먼저 읽는다.
>
> 규칙과 문서 라우팅은 [CLAUDE.md](CLAUDE.md), 에셋 대조 이력과 결정 목록은
> [대조결과.md](대조결과.md)에 있다. **여기는 진행 상황만 적는다.**

---

## 1. 무엇을 만들고 있나

대리점이 **직접 A5 전단지를 만드는 웹 도구**다. 지금은 대리점이 Brand Experience팀(김원)에
요청하면 담당자가 만들어 PDF·AI로 보내준다. 그 왕복을 없애는 게 목적이다.

자유 디자인 툴이 아니다. **미리 만들어 둔 블록을 조합**하는 도구다.
브랜드 가이드는 검사로 지키지 않고 **선택지를 가이드 안의 값으로 한정**해 지킨다 —
컬러는 팔레트에서만, 서체는 지정 2종만, 로고는 승인 파일만. 위반이 적발되는 게 아니라
구조적으로 불가능하다.

배경과 기대 효과는 [중간과제_김원.md](중간과제_김원.md)에 있다.

---

## 2. 지금 굴러가는 것

`flyer-studio.html` 하나를 브라우저로 열면 바로 동작한다. 그래픽 190장과 서체 서브셋이
파일 안에 들어 있어 다른 것이 필요 없다.

**화면 3단계**

```
1 유형 선택  →  2 블록 담고 편집  →  3 PDF 만들기
```

원래 4단계였는데 블록 고르기와 편집을 한 화면으로 합쳤다. 담으면서 결과를 바로 보게 하려고.

**유형 3종** — 유형 1 선 분할 · 유형 2 컬러 분할 · 유형 3 타이틀 분할

**블록 카테고리 6종** — 1 요금 안내 · 2 사은품 · 3 요금 안내+상품 · 4 메세지 소구형 ·
5 시즈널 · 6 본부·유통망 요청

**블록 폭** — 12칸 모델. 전면 12 · 1/2 6 · 1/3 4 · 1/4 3.
조합이 12를 넘으면 담기지 않는다.

**그래픽 자동 추천** — 원고 문구와 에셋 `검색어`를 **낱말 경계**로 맞춰 순위를 매긴다.
후보는 블록 카테고리별로 미리 좁혀 둔 풀 안에서만 나온다. 아이콘 141개 전체에서 고르지 않는다.

**내보내기 2종** — 인쇄소 입고용 · 화면 공유용

---

## 3. 건드릴 때 같이 고쳐야 하는 것

**그래픽 순위 규칙은 세 군데에 있다.** 하나만 고치면 터미널 결과와 도구 화면이 어긋난다.

| | |
|---|---|
| 문서 | [wiki/template.md](wiki/template.md) 2.5 |
| 참조 구현 | `wiki/pick.py` |
| 도구 | `studio/Main.dc.html` 의 `gfxRank()` |

빌드 순서와 나머지 규칙은 [CLAUDE.md](CLAUDE.md)에 있다. `studio/`가 원본이고
`flyer-studio.html`은 생성물이다. 번들 안 JSON을 직접 고치지 않는다.

---

## 4. 남은 일

### BX팀 확인이 필요했던 것 — 2026-09-17 전부 완료

[대조결과.md](대조결과.md) 7절에서 '대기'였던 8개 항목(3·5·13·14·17·18·21·23)이 이날 전부
BX 확인을 받고 끝났다. 반영 후 `build.py` · `build-graphics.py` · `sync-studio.py`를 재실행했다.

| # | 내용 | 결과 |
|---|---|---|
| 3 | 목록 외 아이콘 25개 | 19개 검토 → 14개 파일(13슬롯) 등재, 무빙스타일 폐기, 3개(B tv+ 키비주얼·비브랜드컬러 Coffee.png·완전동일 사본) 최종 미등재. [assets-icon.md](wiki/assets-icon.md) |
| 5 | 제품 포토그래피 UUID 8개 | 삼성·애플 공식 색상 대조로 기종 확정, 파일명을 기종명으로 교체(예: `26ef8da0…`→`폴더블(폴드6).webp`). [assets-product.md](wiki/assets-product.md) |
| 13 | 제목체 SemiBold 결번 | **원본 자체가 없다고 확인** — Regular/Bold 대체를 영구 채택. [typography.md](wiki/typography.md) |
| 14 | 2D 아이콘이 전부 B Blue | 오렌지·핑크 버전 없음 확인 — B Blue 그대로 등재, brand.md 6.2 문구도 수정. [assets-line-icon.md](wiki/assets-line-icon.md) |
| 17 | 로고 벡터 원본 없음 | BI·CI 9종 `.ai` 벡터 입고 완료. [assets-logo.md](wiki/assets-logo.md) |
| 18·23 | BI 32mm 규정 vs 납품본 8.7mm 실측 — 4배 차이 | **규정값이 맞다**로 확정. 헤더를 56→147px(A5 대지 기준)로 키우고 BI를 32mm 그대로 적용, 본문 12칸 몫을 680→589px로 재계산 — `studio/Main.dc.html` · `studio/Flyer.dc.html` 둘 다 수정 |
| 21 | BI 풀컬러 `#274395`가 팔레트에 없음 | **B Blue(`#3617CE`)로 확정** — brand.md 1.1 Primary Color. `B_logo_fullcolor_인쇄용.png`를 재채색(벡터 `.ai`는 미반영, 옛 색 그대로) |

> **헤더가 커진 게 가장 큰 파급이다.** BI를 규정값(32mm)대로 키우면서 헤더가 56→147px로
> 3배 가까이 커졌고, 그만큼 본문 블록이 작아졌다(340/170/170 → 295/147/147). 12칸 비율 자체는
> 그대로다. 새 조합을 넣을 때는 이 새 기준 높이로 계산되는지 확인한다.

### 해상도를 잃은 2건 — 필요해지면 원본을 다시 받아야 한다

1차 정리 때 고해상도 쪽을 버렸다. 대형 인쇄물(X배너·플래카드·A2 이상)에 쓸 일이 생기면 막힌다.

- `조금흐림_H.png` 4096×4096 → 유지한 `구름조금_STD.png`는 1400×1400
- `illust_설치기사(남)_HQ.png` 2291×4000 → 유지본은 802×1400
  (HQ본은 가슴에 HNS 로고가 남아 있어 유지할 수 없었다)

---

## 5. 저장소

**https://github.com/wony7896/vmd** (public · main)

`brand guide/` 1.4GB 와 `template/` 300MB 는 GitHub 파일 크기 한도(100MB)를 넘어
`.gitignore`로 빠져 있다. **이 맥에만 있다.** 저장소에는 위키·스튜디오 원본·배포 번들만 있다.

다른 기계에서 `build.py` · `build-graphics.py` · `subset-font.py` 를 돌리려면
`brand guide/` 867MB(위키가 실제로 참조하는 498개)를 따로 옮겨야 한다.
전단지를 만들기만 할 거면 `flyer-studio.html` 하나면 된다.

---

## 6. 되짚을 만한 판단

나중에 "왜 이렇게 했지?" 싶을 것들이다.

**12칸 모델은 실측으로 정했다.** 원본 가이드에 조합의 산술이 없어 처음엔 1/2를 4칸으로
임의로 뒀는데, 실제 판형을 재서 6칸으로 고쳤다. 가이드 예시 A(1/2+1/3+1/3)는 14칸이 되어
불가능해서, 기본 문서를 예시 B(1/2+1/4+1/4)로 바꿨다.

**명도 40 인쇄는 승인받고 넣었다.** brand.md 1.6이 '온라인 전용'으로 적어 둔 값이지만
2026-09-15 BX 승인을 받아 경고 없이 통과시킨다.

**추석·설날 아이콘은 비워 뒀다.** 시즈널 8종 중 두 개가 가이드에 없다. 비슷한 걸 끼워 넣지
않고 대체 없이 두기로 했다. 어울리지 않는 그림을 넣는 것보다 비우는 게 낫다는 원칙이다.
시즈널 기본 그래픽은 선물상자다.

**줄 수는 개행이 아니라 미리보기에 실제로 앉는 줄을 잰다.** 그래서 블록을 옮기면 이전 구성에서
잰 값이 캐시에 남는 버그가 있었다(2026-09-15 수정). 4단계 확인이 같은 값을 읽어서, 멀쩡한
원고가 '본문 3줄'로 잡혀 내보내기가 잠길 수 있었다. `remeasureLines()`로 고쳤다.

**BI 헤더 크기는 실측 대신 규정값으로 되돌렸다(2026-09-17).** 2026-09-15엔 brand.md 2.5의
32mm가 헤더 56px에 안 들어가서 납품본 실측(8.7mm)을 임시로 썼는데, BX 확인 결과 **납품본 쪽이
특이 사례였고 규정값이 맞았다.** 그래서 헤더를 규정에 맞춰 키우는 쪽을 택했다 — 실측을 표준으로
따라가면 다음 납품본이 또 다르게 나올 때마다 규정을 계속 무시하게 된다. 본문이 좁아지는 대가가
있지만 "선택지를 가이드 값으로 한정한다"는 이 도구의 원칙(CLAUDE.md)에는 이쪽이 맞다.
